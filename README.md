Welcome, this is a simple sdl2 interface for linux. 
The user may click on any point of the window to display a drop effect.

Dependency: sdl2.

$ chmod +x ./install_sdl2.sh

$ ./install_sdl.sh

$ ./go.sh drop.c
